{
  "name": "coinos-tests",
  "version": "0.0.2",
  "description": "Integration test for Coinos",
  "scripts": {
    "test": "tape test.js | tap-arc",
    "test-headless": "tape test.js --headless | tap-arc",
    "test-act": "act --workflows ./action-test.yml --job runTestLocal"
  },
  "dependencies": {
    "puppeteer": "^10.1.0",
    "tap-arc": "0.1.2",
    "tape": "^5.3.0",
    "undercoin": "0.0.5-b"
  }
}
